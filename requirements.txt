# --- 1. Frontend & Configuration ---
streamlit>=1.32.0
python-dotenv>=1.0.0
PyYAML>=6.0         # config.py에서 사용됨 (누락되었던 패키지)

# --- 2. LangChain Ecosystem ---
# 기본 코어 및 커뮤니티 기능
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# 텍스트 분할 및 임베딩 모델 (Ollama)
langchain-text-splitters>=0.3.0
langchain-ollama>=0.2.0

# 벡터 DB 연동 (ChromaDB)
langchain-chroma>=0.1.0

# --- 3. Vector Database Core ---
chromadb>=0.5.0
# 리눅스/배포 환경에서 오래된 SQLite 버전을 대체하기 위함
pysqlite3-binary>=0.5.0

# --- 4. Document Processing (ETL) ---
PyMuPDF>=1.24.0     # PDF 텍스트 추출 (fitz)
olefile>=0.47       # HWP(OLE) 파일 구조 분석

# --- 5. Utilities ---
# 데이터 처리 및 타입 검증
pydantic>=2.0.0
numpy>=1.26.0
# Streamlit 등에서 데이터 프레임 처리를 위해 필요할 수 있음
pandas>=2.2.0